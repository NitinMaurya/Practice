package practice.DSA.sparseTable;

public class SparseTable {

  public static void main(String[] args) {
    String description = """
        This data structure is used to answer the range minimum queries in O(1).
        Given Preconditions: 
          - The array must be immutable
          - The operation must be idempotent e.g. minimum(), maximum(), gcd()
          which means the result is not affected by the overlapped intervals
        """;
  }
}
